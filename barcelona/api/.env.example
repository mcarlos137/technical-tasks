# PostgreSQL started by `docker compose up -d` (see docker-compose.yml).
# Leave DATABASE_URL empty to serve data/games.json instead, which holds the same data.
DATABASE_URL=postgres://games:games@localhost:5433/games
PORT=4000
