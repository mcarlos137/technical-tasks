# barcelona_explore (Flutter)

The Explore home and Pick-up games screens. See the [root README](../README.md) for how to run everything.

```bash
flutter pub get
flutter run      # needs the games API running on :4000
flutter test
```
